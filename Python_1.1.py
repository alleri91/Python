name = 'Wojtek'

name = None

x_pos = 5
speed = 2

is_game_over = False

character_name = "Wojtek"

print(type(x_pos))


print (type(x_pos))
# Wojtek to pan swiata, hahahahha

speed = x_pos

print (speed)

game_over = True

not_game_over = not game_over
print (not_game_over)

new_x_pos = x_pos + 5 + speed
print (new_x_pos)

Full_name = character_name + ' Gaska' # Imie + 'gaska'
print(Full_name) 

mod_div_x_pos = x_pos % 3
print (mod_div_x_pos) # dzielenie ktore daje reszte

floor_div_x_pos = x_pos // 3 # pokazuje ile razy cos sie miesci w danej liczbie
print (floor_div_x_pos)

kwadrat_mod_div_x_pos = mod_div_x_pos ** 2  # podnosi do potegi
print (kwadrat_mod_div_x_pos)

x_pos += 29 # xpos = 5 + 29 = 34
# pozwala na modyfikacje kodu bez odwolywania sie do wartosci powtornie
print (kwadrat_mod_div_x_pos)


is_true = 5 < 2 # sprawdza czy cos jest prawdziwe
is_true_rowna = 5 != 2 # sprawdza czy cos jest nie rowne sobie
print (is_true)
zmienna = 'zmienna rowna sie'

Wartosc_zmiennej = Full_name + 'zmienna rowna sie'

print (Wartosc_zmiennej)
